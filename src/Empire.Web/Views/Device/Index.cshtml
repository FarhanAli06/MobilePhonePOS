@model IEnumerable<Empire.Application.DTOs.Device.DeviceSelectionDto>
@{
    ViewData["Title"] = "Device Management";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2><i class="fas fa-mobile-alt me-2"></i>Device Management</h2>
    <div>
        <button type="button" class="btn btn-success" data-bs-toggle="modal" data-bs-target="#createDeviceModal">
            <i class="fas fa-plus me-2"></i>Add New Device
        </button>
    </div>
</div>

<!-- Filter Section -->
<div class="card mb-4">
    <div class="card-body">
        <h5 class="card-title">Filters</h5>
        <form id="filterForm" class="row g-3">
            <div class="col-md-3">
                <label class="form-label">Device Type</label>
                <select class="form-select" name="deviceType">
                    <option value="">All Types</option>
                    <option value="Phone">Phone</option>
                    <option value="Laptop">Laptop</option>
                    <option value="Part">Part</option>
                    <option value="Accessories">Accessories</option>
                </select>
            </div>
            <div class="col-md-3">
                <label class="form-label">Status</label>
                <select class="form-select" name="status">
                    <option value="">All Status</option>
                    <option value="available">Available for Sale</option>
                    <option value="sold">Sold</option>
                    <option value="not-for-sale">Not for Sale</option>
                </select>
            </div>
            <div class="col-md-3">
                <label class="form-label">Brand</label>
                <input type="text" class="form-control" name="brand" placeholder="Enter brand">
            </div>
            <div class="col-md-3">
                <label class="form-label">IMEI/Serial</label>
                <input type="text" class="form-control" name="imei" placeholder="Enter IMEI/Serial">
            </div>
            <div class="col-12">
                <button type="button" class="btn btn-primary" onclick="applyFilters()">
                    <i class="fas fa-search me-2"></i>Apply Filters
                </button>
                <button type="button" class="btn btn-secondary" onclick="clearFilters()">
                    <i class="fas fa-times me-2"></i>Clear
                </button>
            </div>
        </form>
    </div>
</div>

<!-- Devices Table -->
<div class="card">
    <div class="card-body">
        <table class="table table-striped data-table" id="devicesTable">
            <thead>
                <tr>
                    <th>Device Type</th>
                    <th>Brand</th>
                    <th>Model</th>
                    <th>IMEI/Serial</th>
                    <th>Condition</th>
                    <th>Buying Price</th>
                    <th>Selling Price</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var device in Model)
                {
                    <tr>
                        <td>
                            <span class="badge bg-primary">@device.DeviceType</span>
                        </td>
                        <td>@device.Brand</td>
                        <td>@device.Model</td>
                        <td>@device.IMEISerialNumber</td>
                        <td>
                            <small class="text-muted">@device.ConditionSummary</small>
                        </td>
                        <td>@(device.BuyingPrice?.ToString("C") ?? "N/A")</td>
                        <td>@(device.SellingPrice?.ToString("C") ?? "N/A")</td>
                        <td>
                            @if (device.IsSold)
                            {
                                <span class="badge bg-danger">Sold</span>
                            }
                            else if (device.IsAvailableForSale)
                            {
                                <span class="badge bg-success">Available</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">Not for Sale</span>
                            }
                        </td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <button type="button" class="btn btn-outline-primary" onclick="viewDevice(@device.Id)">
                                    <i class="fas fa-eye"></i>
                                </button>
                                <button type="button" class="btn btn-outline-warning" onclick="editDevice(@device.Id)">
                                    <i class="fas fa-edit"></i>
                                </button>
                                @if (!device.IsSold && device.IsAvailableForSale)
                                {
                                    <button type="button" class="btn btn-outline-success" onclick="sellDevice(@device.Id)">
                                        <i class="fas fa-dollar-sign"></i>
                                    </button>
                                }
                                <button type="button" class="btn btn-outline-danger" onclick="deleteDevice(@device.Id)">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<!-- Create Device Modal -->
<div class="modal fade" id="createDeviceModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Add New Device</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form id="createDeviceForm">
                <div class="modal-body">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label">Device Type *</label>
                            <select class="form-select" name="deviceType" required>
                                <option value="">Select Device Type</option>
                                <option value="Phone">Phone</option>
                                <option value="Laptop">Laptop</option>
                                <option value="Part">Part</option>
                                <option value="Accessories">Accessories</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Brand *</label>
                            <input type="text" class="form-control" name="brand" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Model *</label>
                            <input type="text" class="form-control" name="model" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">IMEI/Serial Number</label>
                            <input type="text" class="form-control" name="imeiSerialNumber">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Battery Health (%)</label>
                            <input type="number" class="form-control" name="batteryHealthPercentage" min="0" max="100">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Network Status</label>
                            <select class="form-select" name="networkStatus">
                                <option value="Unlocked">Unlocked</option>
                                <option value="Locked">Locked</option>
                                <option value="Unknown">Unknown</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Scratches</label>
                            <select class="form-select" name="scratchesCondition">
                                <option value="None">None</option>
                                <option value="Minor">Minor</option>
                                <option value="Major">Major</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Buying Price</label>
                            <input type="number" class="form-control" name="buyingPrice" step="0.01" min="0">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Selling Price</label>
                            <input type="number" class="form-control" name="sellingPrice" step="0.01" min="0">
                        </div>
                        <div class="col-12">
                            <label class="form-label">Source</label>
                            <input type="text" class="form-control" name="source" placeholder="Where did you buy this device?">
                        </div>
                        <div class="col-12">
                            <label class="form-label">Notes</label>
                            <textarea class="form-control" name="notes" rows="3" placeholder="Any additional notes about the device condition or issues"></textarea>
                        </div>
                        <div class="col-12">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" name="isAvailableForSale" checked>
                                <label class="form-check-label">Available for Sale</label>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-plus me-2"></i>Add Device & Print Label
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function applyFilters() {
            // Implement filter logic
            console.log('Applying filters...');
        }

        function clearFilters() {
            document.getElementById('filterForm').reset();
            $('#devicesTable').DataTable().search('').draw();
        }

        function viewDevice(id) {
            // Implement view device logic
            console.log('Viewing device:', id);
        }

        function editDevice(id) {
            // Implement edit device logic
            console.log('Editing device:', id);
        }

        function sellDevice(id) {
            // Implement sell device logic
            console.log('Selling device:', id);
        }

        function deleteDevice(id) {
            if (confirm('Are you sure you want to delete this device?')) {
                // Implement delete logic
                console.log('Deleting device:', id);
            }
        }

        // Handle form submission
        document.getElementById('createDeviceForm').addEventListener('submit', function(e) {
            e.preventDefault();
            // Implement create device logic
            console.log('Creating device...');
        });
    </script>
}

